<!DOCTYPE html>

<html>

    <head>
        <title>{{title}}</title>
        <link rel="stylesheet" href="/css/styles.css">
    </head>

    <body>
        <div id="main-content">
            {{>header}}
            <div style="padding:5px"></div>
            <div style="text-align: center;">
                <a class="new-post" href="/submitpost?b=random">{MAKE A NEW POST}</a>
            </div>

            <div class="post-area">
                {{#each pin}}
                    <div class="post-block">
                        <a href="/b/random/{{this._id}}" style="color: #b92828;">{{this.title}}</a>
                    </div>
                {{/each}}
                
                {{#each unpin}}
                    <div class="post-block">
                        <a href="/b/random/{{this._id}}">{{this.title}}</a>
                    </div>
                {{/each}}
            </div>
        </div>

        {{>footer}}
    </body>

</html>