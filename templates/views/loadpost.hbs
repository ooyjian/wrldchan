<!DOCTYPE html>

<html>

    <head>
        <link rel="icon" href="/img/mask-icon.png">
        <link rel="stylesheet" href="/css/styles.css">
        <title>{{title}}</title>
        <script src="/js/reply.js"></script>
    </head>

    <body>
        <div id="main-content">
            {{>header}}
            <div class="post-content-block">
                <h3>{{title}}</h3>
                <HR style="FILTER: alpha(opacity=100,finishopacity=0,style=1)" width="60%" color="#ff9fb2" SIZE=3 align="left">
                <p style="white-space: pre-wrap;">{{content}}</p>
                <button onclick="op_reply()">Reply</button><br>
                <form action="/b/random/{{post_id}}" id="op-reply" method="POST" style="display: none;">
                    <textarea class="reply-area" name="userInput" style="background-color: #363636;"></textarea>
                    <input type="submit" value="Submit">
                </form>
            </div>
            {{#addReply reply}}
                {{>replyFooter}}
            {{/addReply}}
        </div>
        {{>footer}}
    </body>

</html>